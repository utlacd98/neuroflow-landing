import FocusyncDashboard from '@/components/NeuroFlowDashboard';

export const metadata = {
  title: 'FOCUSYNC Dashboard - Start Your Focus Session',
  description: 'Begin an adaptive focus session with real-time audio and activity tracking.',
};

export default function DashboardPage() {
  return <FocusyncDashboard />;
}

